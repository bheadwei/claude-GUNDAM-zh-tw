# 預設：所有文字檔在 repo 內統一使用 LF
* text=auto eol=lf

# Shell scripts 必須 LF（跨平台執行）
*.sh text eol=lf

# Windows 專屬腳本必須 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# ----------------------------------------------------------------------------
# 為什麼這裡**沒有** `merge=union`
#
# 曾經想給 wbs.md 之類的追蹤檔掛 union（git 內建的「兩邊的行都留下、不留衝突
# 標記」driver），理由是平行 worktree 最常撞的就是「兩個 agent 各自標自己那一列 ✅」。
#
# **實測推翻了它。** 兩個相鄰任務各自標 ✅，union 的結果是：
#
#   | 1.1 | A | ✅ |     ← 一邊標的
#   | 1.2 | B | ⏳ |
#   | 1.1 | A | ⏳ |     ← 另一邊那份的舊狀態
#   | 1.2 | B | ✅ |     ← 另一邊標的
#
# 四列、兩份重複、狀態互相矛盾，**而且沒有任何衝突標記**。
# 原因：union 保留的是「兩個版本的整個 diff hunk」，不是「各留自己改的那一行」。
# 相鄰的行會落在同一個 hunk 裡——所以官方警語的「lines depend on each other」
# 門檻比字面更低，**單純相鄰就足以壞掉**。
#
# 而它的失敗模式是**靜默產出錯的結果**，比停下來報衝突更糟。
#
# 真正的解法不在 merge driver，在流程：**wbs.md 只在主 checkout 更新一次**
# （見 commands/verify.md 的「worktree 內不標 WBS」與 commands/worktree.md 的合併迴圈）。
# 衝突的根因是同一份檔案被 N 個 worktree 各改一次——那該從源頭避免，不是靠合併時補救。
# ----------------------------------------------------------------------------

# 二進位檔不處理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.exe binary

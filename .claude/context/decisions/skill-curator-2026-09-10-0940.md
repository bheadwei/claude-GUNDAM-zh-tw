---
agent: skill-curator
date: 2026-09-10-0940
area: decisions
target: .claude/skills/using-taskmaster/SKILL.md（＋主模型後續修的 3 個連帶檔）
severity: medium
status: resolved
---

# 宣告義務改為無條件，路由表的文件列依「描述的對象」拆兩列

## TL;DR
壓力測試唯一明確失敗的一項（01／02 都沒走文件路由，02 連拒派理由都沒有）已收口：
宣告義務從「要派時才宣告」改成「派或不派都要宣告，拒派理由必須指向那個 agent 的產出」。
**接線與措辭已驗，行為改變未驗**——harness 在 `-p` 下測不完 01／02。

## 範圍
- **檢查目標**：`.claude/skills/using-taskmaster/SKILL.md`（`session-start.sh` 全文注入，每 session 常駐）
- **觸發原因**：2026-09-09 第一輪壓力測試實跑的唯一明確失敗項
- **參考資料**：`.claude/tests/skill-compliance/results/20260909-130333/_判讀.md`（該目錄不進版控）

## 發現

### 🔴 Critical / High
- `SKILL.md:22` — 舊措辭只規定「要派時」怎麼宣告，等於默許「不派 → 不用宣告」。
  **prompt 02 就是從這個缺口掉出去的：它的失敗不是「想錯了」，是連判斷都沒發生。**
  Red Flags 那 11 列全部在反駁「你正在想的某個念頭」，02 沒產生任何念頭，整張表對它零作用
  → 已改為無條件宣告，並要求拒派理由指向「那個 agent 的產出在這情境為什麼沒用」，
  附 prompt 07 的實例當形狀（07 是正解示範：明確拒派 architect 並說明理由）
- `SKILL.md:41` — 文件路由只有一列指向 `documentation-specialist`，但改到 **`.claude/` 本身**時
  該派的是 `skill-curator`（`documentation-specialist` 的職責是從程式碼更新 codemap／API 文檔，
  它不懂 `.claude/` 的接線）。這是 prompt 01 暴露的真實缺口
  → 依「文件描述的對象」拆兩列，不依副檔名

### 🟡 Medium（agent 回報、主模型當場驗證後修掉）
- `SKILL.md:11` — 寫「本專案有 14 個專業 subagent」，實際 16 → 已改。
  `check-counts.sh` 抓不到它（只比標題數字與目錄樹，不比內容），所以 313 項全綠仍留著這個
- `.claude/tests/skill-compliance/README.md:52` — 01 的期望行為寫死 `documentation-specialist`，
  拆列後在模板 repo 內的正解是 `skill-curator`。**不改的話下一輪會把正確行為判成 FAIL**
  → 已改成「依描述的對象選人，**有沒有宣告才是判準**，選錯人不算 FAIL」
- `.claude/rules/agent-orchestration.md:12` — 核心原則 4「委派前先宣告」與新的唯一來源相左，
  同樣默許「不派 → 不用宣告」。兩份都常駐、說法不一致，正是 `writing-extensions` 警告的
  「同一件事放兩層然後漂開」 → 已改為「判定後先宣告，派或不派都要」＋指向唯一來源

### 🟢 Low / Info
- `agent-orchestration.md:35` 的接力名單 11＋3＝14，缺 `skill-curator`
  （它會寫報告也會建 handoff，`post-agent-report.sh:60` 有 `AREA="decisions"`）
  → 已補成 12＋3＝15，並**寫明第 16 個 `conflict-resolver` 是刻意排除**
  （沒有 Write 工具，機制上寫不了報告），免得下一個數數的人把它「補」回來
- `.claude/taskmaster-data/.skill-impact` 有 17 筆待審的 `.claude/` 變更未盤點。
  agent 刻意沒清除——清掉等於把維護欠債靜默丟棄

## 未驗證的部分（不是懶得跑，是跑不了）
**行為改變未驗證。** `run-compliance.sh` 的產出寫進 `.claude/tests/skill-compliance/results/`，
且該 harness 會真的改動 tracked 檔案又沒有工作區乾淨檢查；加上 `-p` 非互動模式寫不了
`.claude/**`、跑不了 Bash，而 01／02 正是靠這兩類動作才做得完。
**所以這次交付的是「接線與措辭正確」，不是「模型會照做」。**

## 這次改動新引入的風險（下一輪實跑要盯）
「不派」第一次成為「規則」節裡的**具名選項**。已用「理由必須指向那個 agent 的產出」＋
排除兩句空話來收窄，但**形式正確、實質逃避的拒派宣告（07 的殼、02 的裡子）是這條規則
可能的新失敗模式**，只有正向題跑得起來時才驗得到。

## 建議的後續 Agent
無（未建 handoff）。

## 度量
- 改動檔案數：4（SKILL.md +8/−2 由 agent；其餘 3 檔由主模型驗證後修）
- `check-counts.sh`：313 項全部一致
- `run-tests.sh`：未跑（未動 hook）
